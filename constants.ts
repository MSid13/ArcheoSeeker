
import { ItemType, Era, Region } from './types';

export const ITEM_TYPES: string[] = Object.values(ItemType);
export const ERAS: string[] = Object.values(Era);
export const REGIONS: string[] = Object.values(Region);
